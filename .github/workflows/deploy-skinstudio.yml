name: Deploy SkinStudio Mod

on:
  push:
    branches:
      - master
    paths:
      - "server-mods/SkinStudio/Scripts/main.lua"
      - ".github/workflows/deploy-skinstudio.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Lua and install FTP client
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y lua5.4 lftp
          luac -p server-mods/SkinStudio/Scripts/main.lua

      - name: Deploy SkinStudio
        shell: bash
        env:
          FTP_HOST: ${{ secrets.VERYGAMES_FTP_HOST }}
          FTP_PORT: ${{ secrets.VERYGAMES_FTP_PORT }}
          FTP_USER: ${{ secrets.VERYGAMES_FTP_USERNAME }}
          FTP_PASS: ${{ secrets.VERYGAMES_FTP_PASSWORD }}
        run: |
          set -euo pipefail
          mkdir -p predeploy-backup
          date +%s > /tmp/reload.flag
          lftp -c "
            set cmd:fail-exit yes;
            set ssl:verify-certificate no;
            set net:max-retries 3;
            open -p \"$FTP_PORT\" -u \"$FTP_USER\",\"$FTP_PASS\" \"$FTP_HOST\";
            mkdir -p /TheIsle/Binaries/Win64/ue4ss/Mods/SkinStudio/Scripts;
            mkdir -p /TheIsle/Binaries/Win64/ue4ss/Mods/SkinStudio/Saved;
            set cmd:fail-exit no;
            get /TheIsle/Binaries/Win64/ue4ss/Mods/SkinStudio/Scripts/main.lua -o predeploy-backup/main.lua;
            set cmd:fail-exit yes;
            put server-mods/SkinStudio/Scripts/main.lua -o /TheIsle/Binaries/Win64/ue4ss/Mods/SkinStudio/Scripts/main.lua;
            put /tmp/reload.flag -o /TheIsle/Binaries/Win64/ue4ss/Mods/SkinStudio/Saved/reload.flag;
            bye;
          "

      - name: Upload pre-deploy backup
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: skinstudio-predeploy-${{ github.sha }}
          path: predeploy-backup/main.lua
          if-no-files-found: ignore
          retention-days: 90
